name: zeecv
description: "A professional resume maker app"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # UI
  google_fonts: ^6.1.0
  
  # State Management
  provider: ^6.0.5
  
  # API & Storage
  http: ^1.1.0
  shared_preferences: ^2.2.2
  
  # Navigation
  go_router: ^13.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true